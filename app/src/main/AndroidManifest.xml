<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fengxing.mobile.commonutils">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".FXApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Common_Theme_NoActionBase">
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.Login2Activity" />
        <activity android:name=".KeyBoardHideActivity" />
        <activity android:name=".BaseActivity" />

        <activity android:name="MainActivity" />
        <!--test activity-->
        <activity android:name=".test.activity.TestDeleteListActivity" />

        <activity android:name=".test.activity.TestAnimActivity" />

        <activity android:name=".test.activity.TestTweenAnimActivity" />

        <activity android:name=".TextPermissionActivity" />
        <activity
            android:name=".test.activity.TestStatusBarTransparentActivity"
            android:theme="@style/Common_Theme_NoActionBase" />

        <activity android:name=".test.activity.TestViewDragHelperActivity" />

    </application>

</manifest>